services:
  app:
    build: ./backend
    ports:
      - "7860:7860"
    volumes:
      - ./frontend:/app/frontend:ro
      - ./data:/app/data
    environment:
      - MAX_UPLOAD_MB=50
      - DB_PATH=/app/data/jobs.db
    restart: unless-stopped
